{
	"name": "bruhs",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"dev:design": "turbo run dev --filter=design",
		"dev:www": "turbo run dev --filter=www",
		"dev:domains": "turbo run dev --filter=domains",
		"check:design": "turbo run check --filter=design",
		"check:www": "turbo run check --filter=www",
		"check:domains": "turbo run check --filter=domains"
	},
	"devDependencies": {
		"turbo": "2.5.3"
	},
	"packageManager": "pnpm@10.11.0",
	"engines": {
		"node": ">=20"
	}
}
