{
	"name": "bruhs",
	"private": true,
	"scripts": {
		"dev:bruhs": "turbo run dev --filter=bruhs",
		"dev:llm": "turbo run dev --filter=llm",
		"dev:portfolio": "turbo run dev --filter=portfolio",
		"dev:snip": "turbo run dev --filter=snip",
		"check:bruhs": "turbo run check --filter=bruhs",
		"check:llm": "turbo run check --filter=llm",
		"check:portfolio": "turbo run check --filter=portfolio",
		"check:snip": "turbo run check --filter=snip"
	},
	"devDependencies": {
		"turbo": "2.5.3"
	},
	"packageManager": "pnpm@10.11.0",
	"engines": {
		"node": ">=20"
	}
}
